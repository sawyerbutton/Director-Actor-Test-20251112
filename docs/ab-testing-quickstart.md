# A/B 测试快速入门（3 分钟）

## 🎯 目标

3 分钟内完成第一个 A/B 测试，对比两个 Temperature 设置的效果。

---

## 步骤 1：准备测试数据（30 秒）

使用项目自带的测试数据：

```bash
# 确认文件存在
ls examples/golden/百妖_ep09_s01-s05.json
```

✅ 如果文件存在，继续下一步

---

## 步骤 2：运行 A/B 测试（2 分钟）

执行以下命令：

```bash
python -m src.cli ab-test examples/golden/百妖_ep09_s01-s05.json \
  --temperatures 0.0,0.7
```

**这个命令的含义**：
- 对比两个 Temperature 设置
- `0.0` = 完全确定性（每次结果相同）
- `0.7` = 较高随机性（结果更具创造性）

---

## 步骤 3：查看结果（30 秒）

命令执行后，您将看到类似输出：

```
============================================================
📊 A/B TEST COMPARISON REPORT
============================================================

Variant         Success     Duration     TCCs     Confidence  Errors
--------------------------------------------------------------------------------
temp-0.0        ✅          124.41s       2       95.00%        0
temp-0.7        ✅          131.20s       2       92.50%        0

🏆 Winner: temp-0.0

💡 RECOMMENDATION
================================================================================
Based on the test results, 'temp-0.0' is recommended.
✅ Success rate: 100%
⏱️  Average duration: 124.41s
🎯 TCC confidence: 95.00%
================================================================================
```

**结果解读**：
- ✅ 两个变体都成功完成
- 🏆 **temp-0.0** 获胜（更快 + 更准确）
- 💡 **建议**：生产环境使用 temperature=0.0

---

## 🎉 完成！

您已经成功完成了第一个 A/B 测试。

### 接下来可以尝试：

#### 1. 对比 LLM 提供商

```bash
python -m src.cli ab-test examples/golden/百妖_ep09_s01-s05.json \
  --providers deepseek,anthropic
```

**注意**：需要配置相应的 API Key

#### 2. 对比自定义变体

```bash
python -m src.cli ab-test examples/golden/百妖_ep09_s01-s05.json \
  --variants baseline,experimental
```

#### 3. 多次运行取平均（更准确）

```bash
python -m src.cli ab-test examples/golden/百妖_ep09_s01-s05.json \
  --temperatures 0.0,0.7 \
  --runs 3
```

---

## 💡 常用命令速查

```bash
# 对比 Temperature
python -m src.cli ab-test script.json --temperatures 0.0,0.5,0.7

# 对比提供商
python -m src.cli ab-test script.json --providers deepseek,anthropic

# 对比变体（运行 5 次）
python -m src.cli ab-test script.json --variants v1,v2 --runs 5

# 保存详细结果
python -m src.cli ab-test script.json --variants v1,v2 --output results.json
```

---

## 📚 深入学习

- **完整指南**：[A/B Testing Guide](./ab-testing-guide.md)
- **Python API**：在代码中使用 A/B 测试
- **最佳实践**：如何设计有效的实验

---

**预计时间**：3 分钟
**前置要求**：已安装项目依赖并配置 API Key
**文档版本**：1.0.0
**最后更新**：2025-11-13
