# Testing dependencies for Script Analysis System

# Core testing framework
pytest>=7.4.0
pytest-cov>=4.1.0  # Coverage reporting
pytest-mock>=3.11.1  # Mocking utilities

# Data validation (already in main requirements)
pydantic>=2.0.0

# Optional: For async testing (if needed for LangGraph)
pytest-asyncio>=0.21.0

# Optional: For better test output
pytest-sugar>=0.9.7
pytest-html>=3.2.0
